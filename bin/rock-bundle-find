#! /usr/bin/env ruby
require 'rock/bundle'

name = ARGV.first
Bundles.each_bundle do |bdl|
    if bdl.name == name
        puts bdl.path
        exit 0
    end
end
puts "cannot find required bundle"
exit 1

